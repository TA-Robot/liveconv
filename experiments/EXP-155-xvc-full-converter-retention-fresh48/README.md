# EXP-155: full-converter retention on frozen fresh48

Status: completed; technical reject

Render base, control69, and the unchanged EXP-154 converter on the exact 48
disjoint Common Voice speakers. Reject candidate-added gross repetition and
report common non-gross W/T/L, mean, median, and known-text distance. Audio
remains unheard and unselected.

The candidate added one gross row beyond control69, repeating `ん` 223 times
on `cv39028774f`. Raw source-relative mean regressed `1.001 -> 1.338`. On 45
common non-gross rows it scored W/T/L `9/25/11`, mean `0.326 -> 0.353`, and
median `0.250 -> 0.294`. Known-text mean was essentially tied
`0.610 -> 0.606`, which cannot override the new corruption or select quality.
