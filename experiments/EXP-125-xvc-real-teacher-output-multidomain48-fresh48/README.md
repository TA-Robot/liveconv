# EXP-125: cross-corpus full-output teacher on frozen fresh48

Status: prepared; evaluation-only

Render base, EXP-035 control69, and EXP-124 once on the exact EXP-112 manifest.
Fresh48 remains evaluation-only and no fresh48 row enters training. Reject on an
adapter-added gross loop or broad regression on common non-loop rows. Only a
The JVS/Hadou condition matrix and disjoint Hadou31 are separate required
diagnostic screens, not gates that can rescue a fresh48 corruption failure.
Only a survivor may enter the larger frozen stress60. Auxiliary ASR is a
corruption/content screen, not a naturalness, target-voice, or winner metric.
