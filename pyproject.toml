[project]
name = "liveconv-workspace"
version = "0.1.0"
description = "Workspace tooling for the liveconv remote audio lab"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "httpx>=0.28,<1",
  "jsonschema==4.25.1",
  "pytest>=8,<9",
  "pytest-asyncio>=0.25,<2",
  "ruff==0.16.2",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
  "experiments/EXP-002-remote-router/runner",
  "experiments/EXP-003-real-model-route",
  "experiments/EXP-004-rvc-gateway-smoke",
  "experiments/EXP-005-extension-multimodel-mvp",
  "packages/protocol",
  "packages/evaluation",
  "packages/stt",
  "packages/speaker",
  "services/audio",
  "workers",
]

[tool.pytest.ini_options]
addopts = "-ra --import-mode=importlib"
markers = [
  "live: opt-in test requiring a disposable running Gateway",
]
testpaths = [
  "packages/protocol/tests",
  "packages/evaluation/tests",
  "packages/stt/tests",
  "packages/speaker/tests",
  "experiments/EXP-002-remote-router/runner/tests",
  "experiments/EXP-003-real-model-route/exp003_tests",
  "experiments/EXP-004-rvc-gateway-smoke/exp004_tests",
  "experiments/EXP-005-extension-multimodel-mvp/exp005_tests",
  "scripts/tests",
  "services/audio/tests",
  "workers/tests",
  "workers/runtime/tests",
  "workers/adapters/beatrice_2/tests",
  "workers/adapters/openvoice_v2/tests",
  "workers/adapters/rvc_v2/tests",
  "workers/adapters/x_vc/tests",
]

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
